import { describe, it } from 'vitest';

describe('E2E smoke', () => {
	it.skip('должен проходить сквозной запуск _run.js на фикстурах', async () => {
		// Шаблон для будущего e2e:
		// 1) Подготовить фикстуры во временной директории.
		// 2) Подменить SMTP-отправку (без реального сетевого вызова).
		// 3) Запустить сценарий и проверить итоговый email.json.
	});
});
