# 2033.email

Если в ".env" присутствует "debug_to", то отправка происходит на этот email иначе на email из "на_отправку\лицевой\email.json".

# .env

smtp_server = 'smtp.ukr.net'

smtp_user = '...@ukr.net'

smtp_pwd = '...'

#debug_to = "...@ukr.net"
